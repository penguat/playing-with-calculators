{
  "name": "playing_with_calculators",
  "version": "0.1.0",
  "description": "Alex playing with calculators, just messing around really",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage",
    "build": "tsc && browserify out/index.js -o publish/bundle.js"
  },
  "author": "Alex Addison",
  "license": "WTFPL",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.0.5",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "browserify": "^17.0.0"
  }
}
